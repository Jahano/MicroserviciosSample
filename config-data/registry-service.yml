server:
  port: 7084

eureka:
  instance:
    hostname: localhost
  ckient:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.intance.hostname}:${server.port}/eureka
